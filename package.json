{
    "scripts": {
        "heroku-prebuild": "cd frontend && npm install && cd .. && cd backend && npm install",
        "start": "cd backend && npm start"
    },
    "engines": {
        "node": "14.x"
    }
}